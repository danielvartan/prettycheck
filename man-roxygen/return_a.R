#' @return
#' - `test_*`: `TRUE` if it passes the test; `FALSE` otherwise.
#' - `check_*`: An [invisible][invisible()] `TRUE` if it passes the test;
#'    a warning message otherwise.
#' - `assertion_*`: An [invisible][invisible()] `TRUE` if it passes the test;
#'    an error message otherwise.
#' - `expect_*`: An [invisible][invisible()] `TRUE` if it passes the test;
#'    an error message otherwise.
