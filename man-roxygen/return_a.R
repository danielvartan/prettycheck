#' @return
#' - `test_*`: `TRUE` if it passes the test; `FALSE` otherwise.
#' - `check_*`: The same input as [invisible][invisible()] if it passes the
#'    test; a warning message otherwise.
#' - `assertion_*`: The same input as [invisible][invisible()] if it passes the
#'    test; an error message otherwise.
#' - `expect_*`: The same input as [invisible][invisible()] if it passes the
#'    test; an error message otherwise.
