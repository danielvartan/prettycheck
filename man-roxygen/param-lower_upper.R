#' @param lower (Optional) A value of the same class as `x` indicating
#'   the minimum value of `x` (Default: `-Inf`).
#' @param upper (Optional) A value of the same class as `x` indicating
#'   the maximum value of `x` (Default: `Inf`).
