#' @return
#' - `test_*`: `TRUE` if it passes the test; `FALSE` otherwise.
#' - `check_*`: `TRUE` if it passes the test; a string with a message otherwise.
#' - `assertion_*`: An [invisible][base::invisible] `TRUE` if it passes the
#'   test; an error message otherwise.
#' - `expect_*`: An [invisible][base::invisible] `TRUE` if it passes the
#'   test; an error message otherwise.
