#' @return
#' - `test_*`: `TRUE` if it passes the test; `FALSE` otherwise.
#' - `assertion_*`: The same input (as [invisible][base::invisible]) if it
#'    passes the test; an error message otherwise.